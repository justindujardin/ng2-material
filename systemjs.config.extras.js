/**
 * Add barrels and stuff
 * Adjust as necessary for your application needs.
 */
(function (global) {
  System.config({
    packages: {
      'dist': {
        defaultExtension: 'js'
      },
      'src': {
        defaultExtension: 'js'
      },
      // add packages here
    }
  });
})(this);
